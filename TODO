抽奖时，抽到已有车辆，再处置车辆弹窗中，有概率卡死，我不知道为什么

进行一次完整流程，检查有哪些函数没有调用，覆盖率分析？然后清除无用的函数，清理代码。

log分级，或许使用框架？

设置技能点上限为999

目前主要还用于障碍物/特殊非文本目标，例如 recovery 里的 images/obstacles/*。这部分还没完全重构掉。考虑去除这部分。

OCR自动识别CR并计算买车数量

同理，自动识别技能点
